[phases.setup]
nixPkgs = ["nodejs", "python311", "pip"]

[phases.install]
cmds = [
    "cd frontend && npm ci",
    "pip install -r backend/requirements.txt"
]

[phases.build]
cmds = [
    "cd frontend && npm run build"
]

[start]
cmd = "python backend/main.py --port $PORT"